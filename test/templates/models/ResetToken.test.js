var testHelper = require('../../test_helper');
var should = testHelper.should;
var connectorLock = testHelper.connectorLock_local;
var proxyquire = testHelper.proxyquire;

describe('models', function(){
  // var token = proxyquire('../lib/templates/models/ResetToken.js', {'connectorLock': connectorLock});
  // describe('token', function(){
  //   it('should exist', function(done){
  //     token.should.be.Object;
  //     done();
  //   });
  // });
});